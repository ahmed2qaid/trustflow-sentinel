# TrustFlow Sentinel Environment Variables Template
# Server-side integration secrets (Nutrient, SerpApi) are configured separately
# in the backend (Xano).

# Frontend Configuration (apps/web)
VITE_BACKEND_PROVIDER=xano
VITE_API_BASE_URL=https://x8ki-letl-twmt.n7.xano.io/api:soDKl_ie
